sudo apt update
sudo apt upgrade
sudo apt install terminator emacs ncdu tree git python3-dev zsh net-tools glances curl vlc openssh-client openssh-server fonts-powerline cmake chromium-browser build-essential 
sudo apt install texlive-full 
curl https://sh.rustup.rs -sSf | sh
mkdir ~/.emacs.d
mv init.el ~/.emacs.d/init.el
sh -c "$(curl -fsSL https://raw.githubusercontent.com/robbyrussell/oh-my-zsh/master/tools/install.sh)"
git clone https://github.com/denysdovhan/spaceship-prompt.git "$ZSH_CUSTOM/themes/spaceship-prompt"
ln -s "$ZSH_CUSTOM/themes/spaceship-prompt/spaceship.zsh-theme" "$ZSH_CUSTOM/themes/spaceship.zsh-theme"
sed -i 's/ZSH_THEME="robbyrussell"/ZSH_THEME="spaceship"/' ~/.zshrc
